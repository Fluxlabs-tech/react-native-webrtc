#ifdef __cplusplus

#import <React/RCTViewComponentView.h>

/**
 * RTCView's native view (Fabric). It hosts an RTCVideoView and passes it props, events and
 * commands.
 */
@interface RTCVideoViewComponentView : RCTViewComponentView
@end

#endif
