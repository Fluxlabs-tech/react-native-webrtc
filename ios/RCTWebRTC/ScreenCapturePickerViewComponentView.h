#ifdef __cplusplus

#import <React/RCTViewComponentView.h>

/**
 * ScreenCapturePickerView's native view (Fabric): the system broadcast picker, which the show
 * command opens. Empty on tvOS, which has no picker.
 */
@interface ScreenCapturePickerViewComponentView : RCTViewComponentView
@end

#endif
